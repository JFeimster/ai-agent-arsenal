# Manual Upload Guide

Use this file to document how to manually upload generated files into the correct repo folders.

General rule:

- Upload experimental exports into `variants/`.
- Upload markdown source into `content/`.
- Upload shared JSON into `data/`.
- Promote only reviewed winners into `canonical/`.
